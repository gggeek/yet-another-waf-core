HTTPSERVER_PORT=1082
# Set to an empty string to disable testing this, eg. when running the test against Apache
HTTPSERVER_SOCKET=/run/frankenphp/caddy.server.sock

PROXY_PORT=8082
# Set to an empty string to disable testing this, eg. when running the test against Apache
PROXY_SOCKET=/run/frankenphp/caddy.proxy.sock
