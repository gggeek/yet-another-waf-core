HTTPSERVER_PORT=
HTTPSERVER_SOCKET=/run/nginx.server.sock

PROXY_PORT=8080
# Set to an empty string to disable testing this, eg. when running the test against Apache
PROXY_SOCKET=/run/nginx.proxy.sock
